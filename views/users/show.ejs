<%- include('../partials/boilerplate') %>
<%- include('../partials/nav') %>
<div class="user-profile">
    <h3>User Single View</h3>
    <p><%= user.username %></p>
    <p><%= user.email %></p>
    <p><%= user.name %></p>
    <p>Country List</p>
    <% if (userNav === user) { %>
        <a href="/user/<%= userNav.id %>"><button>Edit</button></a>
    <% } %>
</div>
<%- include('../partials/end') %>